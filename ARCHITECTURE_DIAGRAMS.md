# 🏗️ Intervuu - Architecture & Data Flow Diagrams

## System Architecture Overview

```
┌─────────────────────────────────────────────────────────────────────┐
│                     CLIENT SIDE (React + TypeScript)                │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  ┌──────────────────────────────────────────────────────────────┐  │
│  │                    React Components                          │  │
│  │  ┌─────────────────────────────────────────────────────────┐ │  │
│  │  │  App.tsx (Main Router)                                 │ │  │
│  │  │  ├─ LandingPage                                        │ │  │
│  │  │  ├─ AuthScreen                                         │ │  │
│  │  │  ├─ SetupScreen                                        │ │  │
│  │  │  ├─ LobbyScreen (Home)                                 │ │  │
│  │  │  ├─ BookInterviewScreen                                │ │  │
│  │  │  ├─ SpeechInterviewScreen (Interview)                  │ │  │
│  │  │  ├─ InterviewerDashboard                               │ │  │
│  │  │  └─ MessagingScreen                                    │ │  │
│  │  └─────────────────────────────────────────────────────────┘ │  │
│  └──────────────────────────────────────────────────────────────┘  │
│                           ↓                                         │
│  ┌──────────────────────────────────────────────────────────────┐  │
│  │               React Hooks (Business Logic)                   │  │
│  │  ┌──────────────────┐          ┌──────────────────┐          │  │
│  │  │   useAuth.ts     │          │ useInterview.ts  │          │  │
│  │  │ - userId         │          │ - currentSession │          │  │
│  │  │ - isAuthReady    │          │ - isLoading      │          │  │
│  │  │ - logout()       │          │ - startInterview │          │  │
│  │  │ - error          │          │ - submitAnswer   │          │  │
│  │  │                  │          │ - nextQuestion   │          │  │
│  │  │                  │          │ - finishInterview│          │  │
│  │  └──────────────────┘          └──────────────────┘          │  │
│  └──────────────────────────────────────────────────────────────┘  │
│                           ↓                                         │
│  ┌──────────────────────────────────────────────────────────────┐  │
│  │              Services (Data & API Layer)                     │  │
│  │  ┌──────────────────────────────────────────────────────┐   │  │
│  │  │  Firebase & Database Services                        │   │  │
│  │  │  ├─ firebase.ts (Auth initialization)               │   │  │
│  │  │  ├─ firestore.ts (CRUD operations)                  │   │  │
│  │  │  ├─ booking.ts (Booking management)                 │   │  │
│  │  │  └─ availability.ts (Schedule)                      │   │  │
│  │  └──────────────────────────────────────────────────────┘   │  │
│  │  ┌──────────────────────────────────────────────────────┐   │  │
│  │  │  AI & Evaluation Services                            │   │  │
│  │  │  ├─ gemini-api.ts (Question generation)              │   │  │
│  │  │  ├─ interview-questions.ts (Question bank)           │   │  │
│  │  │  └─ resume-analyzer.ts (Resume parsing)              │   │  │
│  │  └──────────────────────────────────────────────────────┘   │  │
│  │  ┌──────────────────────────────────────────────────────┐   │  │
│  │  │  Feature Services                                    │   │  │
│  │  │  ├─ payment.ts (Stripe)                              │   │  │
│  │  │  ├─ messaging.ts (Chat)                              │   │  │
│  │  │  ├─ video-conferencing.ts (Video calls)              │   │  │
│  │  │  ├─ rating.ts (Reviews)                              │   │  │
│  │  │  ├─ notifications.ts (Alerts)                        │   │  │
│  │  │  ├─ analytics.ts (Stats)                             │   │  │
│  │  │  └─ favorites.ts (Saved items)                       │   │  │
│  │  └──────────────────────────────────────────────────────┘   │  │
│  └──────────────────────────────────────────────────────────────┘  │
│                           ↓                                         │
│  ┌──────────────────────────────────────────────────────────────┐  │
│  │  Utils (Helpers, Logging, Error Handling)                    │  │
│  │  ├─ logger.ts (Pino structured logging)                     │  │
│  │  ├─ error-handler.ts (Error normalization)                  │  │
│  │  ├─ retry.ts (Exponential backoff)                          │  │
│  │  ├─ validation.ts (Zod schemas)                             │  │
│  │  └─ constants.ts (App-wide constants)                       │  │
│  └──────────────────────────────────────────────────────────────┘  │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
                              ↓
         ┌────────────────────┼────────────────────┐
         ↓                    ↓                    ↓
    ┌─────────┐          ┌─────────┐         ┌──────────┐
    │Firebase │          │ Gemini  │         │ Stripe   │
    │ Auth    │          │   API   │         │ Payment  │
    │+Firestore          │         │         │          │
    │ Database│          │ AI      │         │ Process  │
    └─────────┘          │Question │         │ Payments │
                         │Gen+Eval │         └──────────┘
                         └─────────┘
```

---

## Authentication Flow Diagram

```
┌──────────────────┐
│  App Loads       │
└────────┬─────────┘
         ↓
┌──────────────────────────────┐
│ Firebase initializes auth    │
│ Checks localStorage for      │
│ previous session token       │
└────────┬─────────────────────┘
         ↓
    ┌────────────────┐
    │ Was user logged│
    │ in before?     │
    └────┬───────┬───┘
         │ YES   │ NO
         ↓       ↓
    ┌────────┐  ┌─────────────────────┐
    │Restore │  │ Show Landing Page   │
    │Session │  │                     │
    └────┬───┘  │ User clicks         │
         │      │ "Get Started"       │
         │      └──────────┬──────────┘
         ↓                 ↓
    ┌──────────────────────────────────┐
    │ Show Auth Screen                 │
    │ (Email/Anonymous login options)  │
    └──────────┬───────────────────────┘
               ↓
    ┌──────────────────────────────────┐
    │ User signs in                    │
    │ Firebase creates user document   │
    │ (uid = user ID)                  │
    └──────────┬───────────────────────┘
               ↓
    ┌──────────────────────────────────┐
    │ App loads user profile from      │
    │ Firestore (users/{uid})          │
    └──────────┬───────────────────────┘
               ↓
         ┌─────────────┐
         │ Has profile?│
         └──┬────────┬─┘
        YES │        │ NO
            ↓        ↓
      ┌──────────┐  ┌──────────────────┐
      │Go to     │  │ Show Setup Screen│
      │Lobby     │  │ (profile creation)
      │(Home)    │  └──────────┬───────┘
      └──────────┘             ↓
                       ┌──────────────────┐
                       │ User fills role, │
                       │ skills, email    │
                       └──────────┬───────┘
                                  ↓
                            ┌──────────┐
                            │Save to DB│
                            │Go to Home│
                            └──────────┘
```

---

## Interview Execution Flow

```
┌──────────────────────┐
│ User clicks          │
│ "Start Interview"    │
└──────────┬───────────┘
           ↓
   ┌───────────────────────────────────┐
   │ useInterview.startInterview()     │
   │ ├─ Initialize session object      │
   │ ├─ Create first question:         │
   │ │  "Introduce yourself..."        │
   │ ├─ Save session to Firestore      │
   │ └─ Update React state             │
   └───────────┬───────────────────────┘
               ↓
   ┌────────────────────────────────────┐
   │ Show SpeechInterviewScreen         │
   │ Display: [Question] [User answer]  │
   │ Display: "Submit Answer" button    │
   └────────────┬─────────────────────┘
                ↓
   ┌────────────────────────────────────┐
   │ User types answer                  │
   │ Clicks "Submit Answer"             │
   └────────────┬─────────────────────┘
                ↓
   ┌────────────────────────────────────┐
   │ useInterview.submitAnswer()        │
   │ ├─ Get user's typed answer        │
   │ ├─ Send to Gemini API             │
   │ └─ Set loading = true             │
   └────────────┬─────────────────────┘
                ↓
   ┌────────────────────────────────────┐
   │ geminiApiService.evaluateAnswer() │
   │ ├─ API Call to Gemini             │
   │ ├─ Returns: score + feedback      │
   │ └─ (with retry logic)             │
   └────────────┬─────────────────────┘
                ↓
   ┌────────────────────────────────────┐
   │ Show feedback to user              │
   │ ├─ Score (1-5)                    │
   │ ├─ Feedback text                   │
   │ └─ Improvement suggestions         │
   │                                    │
   │ Display: "Next Question" button    │
   └────────────┬─────────────────────┘
                ↓
   ┌────────────────────────────────────┐
   │ User clicks "Next Question"        │
   └────────────┬─────────────────────┘
                ↓
         ┌──────────────┐
         │ Questions    │
         │ count check  │
         └──┬────────┬──┘
            │ <5     │ =5
            ↓        ↓
    ┌──────────┐  ┌──────────────────────┐
    │Call next │  │ useInterview.finish  │
    │question  │  │ Interview()          │
    │from      │  │ ├─ Calculate avg     │
    │Gemini    │  │ │  score            │
    │Loop back │  │ ├─ Save session to DB│
    │to display   │ ├─ Mark "completed" │
    └──────────┘  │ └─ Update state     │
                  └──────────┬──────────┘
                             ↓
                  ┌──────────────────────┐
                  │ Show FeedbackModal   │
                  │ ├─ Overall score     │
                  │ ├─ All Q&A summary   │
                  │ ├─ Feedback per Q    │
                  │ └─ "Exit" button     │
                  └──────────┬──────────┘
                             ↓
                  ┌──────────────────────┐
                  │ User clicks "Exit"   │
                  │ Go back to Lobby     │
                  └──────────────────────┘
```

---

## Booking & Live Interview Flow

```
┌──────────────────────────────┐
│ Candidate clicks             │
│ "Book Interview"             │
└──────────────┬───────────────┘
               ↓
   ┌────────────────────────────────────┐
   │ Show BookInterviewScreen           │
   │ ├─ Fetch available interviewers    │
   │ │  from Firestore                  │
   │ ├─ Filter by skills, rating, avail │
   │ └─ Display list with avatars       │
   └────────────┬─────────────────────┘
                ↓
   ┌────────────────────────────────────┐
   │ Candidate selects interviewer      │
   │ ├─ Chooses time slot               │
   │ ├─ Selects duration (30/45/60 min) │
   │ ├─ Chooses difficulty (basic/etc)  │
   │ └─ Enters focus topics (optional)  │
   └────────────┬─────────────────────┘
                ↓
   ┌────────────────────────────────────┐
   │ Clicks "Request Interview"         │
   │ bookingService.createBooking()     │
   └────────────┬─────────────────────┘
                ↓
   ┌────────────────────────────────────┐
   │ Save booking to Firestore:         │
   │ {                                  │
   │   candidateId, interviewerId,      │
   │   scheduledDateTime, duration,     │
   │   skills, difficulty,              │
   │   status: "pending",               │
   │   paymentStatus: "pending"         │
   │ }                                  │
   └────────────┬─────────────────────┘
                ↓
   ┌────────────────────────────────────┐
   │ Payment Step:                      │
   │ Show PaymentCheckout component     │
   │ ├─ Display amount                  │
   │ ├─ Stripe payment form             │
   │ └─ Process payment                 │
   └────────────┬─────────────────────┘
                ↓
         ┌──────────────────┐
         │ Payment approved?│
         └──┬────────────┬──┘
        YES │            │ NO
            ↓            ↓
    ┌──────────────┐  ┌────────────────┐
    │Update booking│  │Show error      │
    │status:       │  │Allow retry     │
    │"accepted"    │  └────────────────┘
    │              │
    │Send notif to │
    │interviewer:  │
    │"New booking" │
    └──────┬───────┘
           ↓
    ┌──────────────────────────────────┐
    │ Interviewer receives notification│
    │ Can "Accept" or "Decline"        │
    └──────────┬───────────────────────┘
               ↓
         ┌──────────────┐
         │ Interviewer  │
         │ accepted?    │
         └──┬────────┬──┘
        YES │        │ NO
            ↓        ↓
    ┌──────────────┐  ┌────────────────┐
    │Generate      │  │Booking marked  │
    │meeting link  │  │"cancelled"     │
    │(Daily.co)    │  │Refund candidate│
    │              │  └────────────────┘
    │Send to both  │
    │parties       │
    │              │
    │Status:       │
    │"confirmed"   │
    └──────┬───────┘
           ↓
    ┌──────────────────────────────────┐
    │ 24 hours before:                 │
    │ Send reminder notification       │
    └──────────┬───────────────────────┘
               ↓
    ┌──────────────────────────────────┐
    │ 1 hour before:                   │
    │ Send reminder notification       │
    │ "Interview starting soon!"       │
    └──────────┬───────────────────────┘
               ↓
    ┌──────────────────────────────────┐
    │ Interview time arrives:          │
    │ Both can click "Join Call"       │
    │ Opens video meeting link         │
    │ (Daily.co embed or Zoom)         │
    └──────────┬───────────────────────┘
               ↓
    ┌──────────────────────────────────┐
    │ Video Interview:                 │
    │ ├─ Call records (optional)       │
    │ ├─ Both can see each other       │
    │ └─ Can end/leave anytime         │
    └──────────┬───────────────────────┘
               ↓
    ┌──────────────────────────────────┐
    │ After interview:                 │
    │ Status: "completed"              │
    │                                  │
    │ Both can leave ratings/reviews:  │
    │ ├─ SubmitReviewScreen            │
    │ ├─ Save rating to Firestore      │
    │ └─ Update interviewer avg rating │
    └──────────────────────────────────┘
```

---

## State Management Flow

```
┌──────────────────────────────────┐
│         Global App State         │
│      (in App.tsx component)      │
└──────────────────────────────────┘
           ↙    ↓     ↖
      ┌────────┴────────┐
      │                 │
      ↓                 ↓
  ┌────────────┐   ┌──────────────┐
  │ useAuth    │   │useInterview  │
  │  Hook      │   │  Hook        │
  ├────────────┤   ├──────────────┤
  │ userId     │   │currentSession│
  │isAuthReady │   │isLoading     │
  │isAuth'd    │   │error         │
  │error       │   └──────────────┘
  │logout()    │
  └─────┬──────┘
        ↓
  ┌──────────────────────────────┐
  │   Services (Singletons)      │
  ├──────────────────────────────┤
  │• firebase (Auth)             │
  │• firestore (Database)        │
  │• geminiApi (AI)              │
  │• booking (Schedule)          │
  │• payment (Stripe)            │
  │• messaging (Chat)            │
  │• ... (8+ more services)      │
  └──────────────────────────────┘
        ↓
  ┌──────────────────────────────┐
  │  External APIs & Database    │
  ├──────────────────────────────┤
  │• Firebase Auth               │
  │• Firestore (Real-time)       │
  │• Gemini API                  │
  │• Stripe API                  │
  │• Daily.co (Video)            │
  └──────────────────────────────┘

Data Flow Example (Simple):
┌─────────────┐
│ User clicks │
│ button      │
└──────┬──────┘
       ↓
┌─────────────────────────────┐
│ Component calls hook method │
│ e.g., useInterview.start()  │
└──────┬──────────────────────┘
       ↓
┌──────────────────────────┐
│ Hook calls service       │
│ e.g., geminiApi.genQ()   │
└──────┬───────────────────┘
       ↓
┌──────────────────────────┐
│ Service makes API call   │
│ e.g., Gemini API         │
└──────┬───────────────────┘
       ↓
┌──────────────────────────┐
│ Service gets response    │
│ Updates React state      │
└──────┬───────────────────┘
       ↓
┌──────────────────────────┐
│ Component re-renders     │
│ with new data            │
└──────────────────────────┘
```

---

## Database Schema (Firestore)

```
firestore/
│
├─ users/{uid}
│  ├─ id: string
│  ├─ uid: string (Firebase UID)
│  ├─ userType: 'candidate' | 'interviewer'
│  ├─ role: string (e.g., "Senior React Dev")
│  ├─ skills: string[] (e.g., ["React", "TypeScript"])
│  ├─ email: string
│  ├─ resumeUrl: string (Firebase Storage path)
│  ├─ createdAt: Timestamp
│  ├─ updatedAt: Timestamp
│  │
│  ├─ interviewerProfile (if interviewer):
│  │  ├─ yearsOfExperience: number
│  │  ├─ specializations: string[]
│  │  ├─ bio: string
│  │  ├─ hourlyRate: number
│  │  ├─ rating: number (1-5)
│  │  ├─ totalInterviews: number
│  │  └─ verified: boolean
│  │
│  └─ candidateProfile (if candidate):
│     ├─ targetRoles: string[]
│     ├─ experienceLevel: string
│     └─ timezone: string
│
├─ interviews/{sessionId}
│  ├─ id: string
│  ├─ userId: string
│  ├─ bookingId: string (optional)
│  ├─ role: string
│  ├─ skills: string[]
│  ├─ date: Timestamp
│  ├─ score: number (avg 1-5)
│  ├─ duration: number (seconds)
│  ├─ status: 'in-progress' | 'completed' | 'abandoned'
│  │
│  └─ questions: [
│     {
│       qText: string,
│       isCoding: boolean,
│       answerText: string,
│       feedback: string,
│       score: number,
│       improvementSuggestions: string[]
│     }, ...
│    ]
│
├─ bookings/{bookingId}
│  ├─ id: string
│  ├─ candidateId: string
│  ├─ interviewerId: string
│  ├─ type: 'ai' | 'live'
│  ├─ scheduledDateTime: Timestamp
│  ├─ durationMinutes: 30 | 45 | 60
│  ├─ role: string
│  ├─ skills: string[]
│  ├─ difficulty: 'basic' | 'intermediate' | 'advanced'
│  ├─ status: 'pending' | 'accepted' | 'confirmed' | 'completed' | 'cancelled'
│  ├─ meetingLink: string
│  │
│  └─ payment:
│     ├─ amount: number
│     ├─ status: 'pending' | 'completed' | 'refunded'
│     ├─ stripeIntentId: string
│     └─ transactionId: string
│
├─ messages/{conversationId}
│  ├─ messages/{messageId}
│  │  ├─ senderId: string
│  │  ├─ text: string
│  │  ├─ timestamp: Timestamp
│  │  └─ read: boolean
│  │
│  └─ participants: [candidateId, interviewerId]
│
├─ ratings/{ratingId}
│  ├─ bookingId: string
│  ├─ interviewerId: string
│  ├─ candidateId: string
│  ├─ score: number (1-5)
│  ├─ review: string
│  ├─ createdAt: Timestamp
│  │
│  └─ interviewerRating (optional):
│     ├─ hireRecommendation: string
│     ├─ strengths: string[]
│     └─ improvements: string[]
│
├─ notifications/{userId}/{notificationId}
│  ├─ type: string
│  ├─ title: string
│  ├─ message: string
│  ├─ read: boolean
│  ├─ actionUrl: string
│  ├─ createdAt: Timestamp
│  └─ expiresAt: Timestamp
│
└─ availability/{interviewerId}/slots/{slotId}
   ├─ dayOfWeek: 0-6
   ├─ startTime: string ("09:00")
   ├─ endTime: string ("17:00")
   └─ timezone: string
```

---

## Component Communication Diagram

```
┌─────────────────────────────────┐
│      App.tsx (Router)           │
│  - Manages screen state         │
│  - Initializes services         │
│  - Provides alert system        │
└──────────────┬──────────────────┘
               │
        ┌──────┴──────┬────────┬───────────┐
        ↓             ↓        ↓           ↓
    ┌────────┐  ┌──────┐  ┌──────┐  ┌─────────────┐
    │LobbyBtn│  │Booking│  │Inter- │  │Interviewer │
    │        │  │Screen │  │viewer │  │Dashboard   │
    │        │  │       │  │Screen │  │            │
    └────┬───┘  └───┬───┘  └───┬──┘  └──────┬──────┘
         │          │          │            │
         │   ┌──────┴──────┬───┴────┬───────┘
         │   │             │        │
         ↓   ↓             ↓        ↓
    ┌──────────────────────────────────────────┐
    │        useAuth Hook                      │
    │ - userId, isAuthReady, logout()          │
    └──────────┬─────────────────────────┬─────┘
               │                         │
               ↓                         ↓
         ┌──────────────┐         ┌──────────────┐
         │firebase.ts   │         │firestore.ts  │
         │(Auth init)   │         │(CRUD ops)    │
         └──────────────┘         └──────────────┘
               │                         │
               └─────────────┬───────────┘
                             ↓
                    ┌────────────────┐
                    │Firebase Firestore
                    │& Cloud Auth    │
                    └────────────────┘
```

---

## Error Handling Flow

```
┌─────────────────────────────┐
│  Component/Hook Operation   │
└──────────────┬──────────────┘
               ↓
         ┌─────────────┐
         │ Try block   │
         └──────┬──────┘
                ↓
         ┌──────────────┐
         │Operation OK? │
         └──┬────────┬──┘
        YES │        │ NO
            ↓        ↓
        Continue  Catch error
                      ↓
               ┌─────────────┐
               │handleError()│
               │- Normalize  │
               │- Log        │
               │- Return     │
               └──────┬──────┘
                      ↓
         ┌────────────────────┐
         │ setError() in state│
         │ OR                 │
         │ showAlert() global │
         └──────────┬─────────┘
                    ↓
         ┌────────────────────┐
         │ UI shows error msg │
         │ with retry option  │
         └────────────────────┘
```

---

## Technology Stack Visualization

```
┌────────────────────────────────────────────┐
│         PRESENTATION LAYER                 │
│  React 19.2 + TypeScript (strict)          │
│  TailwindCSS 4 + Framer Motion             │
│  25+ Reusable Components                   │
│  5+ Full-page Screens                      │
└────────────────────────────────────────────┘
                    ↓
┌────────────────────────────────────────────┐
│         STATE MANAGEMENT                   │
│  React Hooks (useState, useEffect, etc)    │
│  Custom Hooks (useAuth, useInterview)      │
│  Context API (not heavily used)            │
│  Ref-based session tracking                │
└────────────────────────────────────────────┘
                    ↓
┌────────────────────────────────────────────┐
│         BUSINESS LOGIC                     │
│  Services (firebase, firestore, gemini)    │
│  Validation (Zod schemas)                  │
│  Error Handling (centralized)              │
│  Logging (Pino structured logs)            │
│  Retry Logic (exponential backoff)         │
└────────────────────────────────────────────┘
                    ↓
┌────────────────────────────────────────────┐
│         DATA & API LAYER                   │
│  Firebase Auth (Client SDK)                │
│  Firestore (Real-time database)            │
│  Cloud Storage (File management)           │
│  Stripe API (Payments)                     │
│  Gemini 2.5 Flash API (AI)                 │
│  Daily.co API (Video calls)                │
└────────────────────────────────────────────┘
                    ↓
┌────────────────────────────────────────────┐
│         BACKEND (Express)                  │
│  Firebase Admin SDK                        │
│  Rate limiting, CORS, Security             │
│  Webhook handlers                          │
│  Serverless on Vercel                      │
└────────────────────────────────────────────┘
```

---

**These diagrams provide a complete visual understanding of how Intervuu's architecture works!** 🎯
